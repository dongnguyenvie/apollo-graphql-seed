type User {
  id: ID!
  name: String!
  posts: [Post!]!
}

type Token {
  token: String!
}

type ParseToken {
  id: ID
  iat: String
  exp: String
}

type Query {
  user(id: ID!): User!
  login(name: String!, password: String!): Token!
  isLogin: ParseToken
}

type Mutation {
  createUser(name: String!, password: String!): User!
}
